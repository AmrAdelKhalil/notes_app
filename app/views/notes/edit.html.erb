<h1>Editing Note</h1>

<%= render 'form', note: @note, url: user_note_path(current_user, @note) %>

<%= link_to 'Show', user_note_path(current_user, @note) %> |
<%= link_to 'Back', user_note_path(current_user, @note) %>
