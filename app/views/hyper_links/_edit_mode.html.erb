<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <p id="note_title"><%= hyperlink.note.title %></p>
</p>

<p>
  <strong>Body:</strong>
  <p id="note_body"><%= hyperlink.note.body %></p>
</p>

------------------------------------------------------------------
</hr>

<h1>Editing Note Hyperlink</h1>

<%= render 'form', note: hyperlink.note, url:user_note_path(current_user, hyperlink.note) %>


---------------------------------------------------------------------
</hr>
<h1>Most recently collaborators</h1>
<p id="collabs"></p>

<%= link_to 'Back', user_notes_path(current_user) %>

<%= render 'hyper_links/collaboration_handler', hyperlink: hyperlink%>
<%= render 'hyper_links/load_note_body_script', hyperlink: hyperlink, url:  user_note_url(current_user, hyperlink.note.id)%>
<%= render 'hyper_links/sync_input_fields_with_view_script', hyperlink: hyperlink, url:  user_note_url(current_user, hyperlink.note.id)%>
